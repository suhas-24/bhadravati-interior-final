# Bhadravati FINAL -- Coordination Protocol

**Role of this folder:** Shared contracts + status so Dimension Engineer, Color Specialist, Visual Expert, and Orchestrator do not overwrite each other's owned files.

**Authority hierarchy (dimensions):**
1. Signed site tape/laser measure (not yet available) -- fabrication supersedes all.
2. `interior_dimension_control/dimension_register_v1.md` -- **authoritative design baseline for mm**.
3. `coordination/dimension_lock.json` -- machine contract derived from register + MASTER_BRIEF zoning.
4. Rendered PNGs / photos -- **never** invent millimetres from images.

**Authority hierarchy (colour):**
1. Physical sample board under morning / afternoon / 3000 K -- client approval.
2. Birla Opus shade card extract + Century / E3 product codes -- code truth.
3. `coordination/swatch_lock.json` + palette section of `design_tokens.json` -- screen hex contract.
4. Photoreal PNGs -- must not drift; hex on board must match lock.

---

## Ownership matrix

| Owner | Owns exclusively | May read / cite | Must not edit |
|---|---|---|---|
| **Dimension Engineer** | `visuals/floor_plan_precise.svg`, `visuals/axonometric_3d.svg`, `coordination/dimension_lock.json` | register, MASTER_BRIEF, tokens (dims), STATUS | palette SVG, PNGs, index.html, swatch_lock, image_qa |
| **Color Specialist** | `visuals/palette_board.svg`, palette section of `design_tokens.json`, `coordination/swatch_lock.json` | shade extracts, tokens, STATUS | floor/axon SVGs, PNGs, dimension_lock, image_qa |
| **Visual Expert** | all `visuals/*.png`, `visuals/VISUAL_PROMPTS.md`, `coordination/image_qa.json` | locks, brief, QA_FINDINGS | floor/axon/palette SVGs, index.html, dimension/swatch locks |
| **Orchestrator** | `visuals/index.html`, `PRECISION_AUDIT.md`, `QA_FINDINGS.md` residual notes, README sync, final consistency pass, `coordination/PROTOCOL.md`, `coordination/STATUS.md` | all sibling outputs | Do not regenerate owned SVGs/PNGs; merge only |

---

## Hand-off rules

1. **Locks first.** Dimension and Color seed/update `*_lock.json` before claiming SVG/board PASS.
2. **Write status fields** in your lock / `image_qa.json`: `status` ? `draft` | `in_progress` | `ready_for_merge` | `merged`.
3. **Orchestrator merges** only when a sibling marks `ready_for_merge` (or files appear with complete checklist). Merge = update `index.html` captions/swatches/callouts + fill `PRECISION_AUDIT.md` + residual note in `QA_FINDINGS.md`.
4. **No ownership fights.** If an asset is wrong, the owning sibling fixes it; Orchestrator files a FAIL in the audit and waits.
5. **Confidence labels required** on every dimension callout: `register` (K-01/W-01) or `plan_relationship_only` (21x18 studio, furniture approx, openings).
6. **Concept lock (no drift):** Warm Contemporary Minimalism -- NN9074 walls, WW0005 ceiling, all kitchen shutters S1241 MT Latte, black granite counter retained (no waterfall), 80236 TV cabinet only, 84689 Idria Oak four leaves, brushed SS, 3000 K, matte/low-sheen.

---

## Scale contract (SVG)

Locked proposal in `dimension_lock.json`:

- **`1 mm = 0.08 px`** (SVG user units), i.e. scale factor `0.08`.
- Justification: fits overall ~6401x5486 mm plan plus dimension rails and legend inside a ~980-1100 viewBox without crushing callouts; prior schematic used `0.1` (1 unit = 10 mm) and is **out of contract** until Dimension Engineer redraws to `0.08`.
- Formula: `x_px = origin_x + x_mm * 0.08`; `y_px = origin_y - y_mm * 0.08` (screen Y flip).

---

## Done definition (package)

- Dimension lock + floor plan + axon: topology match, register mm labelled, circulation shown.
- Swatch lock + palette board + token hex: codes labelled; Birla hex verified; laminate hex flagged as visual approx if no official RGB.
- `image_qa.json` PASS/FAIL per PNG; failing frames regenerated by Visual Expert.
- Orchestrator: `index.html` consistent, `PRECISION_AUDIT.md` complete, `STATUS.md` = `integration_complete` or lists residuals honestly.
